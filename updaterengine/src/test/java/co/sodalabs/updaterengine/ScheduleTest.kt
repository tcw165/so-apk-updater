package co.sodalabs.updaterengine

import org.junit.Test

class ScheduleTest {

    @Test
    fun `schedule install while it's within the install window`() {
        // TODO
        // ScheduleUtils.findNextInstallTimeMillis()
    }

    @Test
    fun `schedule install while it's outside the install window`() {
        // TODO
        // ScheduleUtils.findNextInstallTimeMillis()
    }
}